x = int(input())         # 시간 복잡도 문제 ( 1 부터 x까지 숫자의 합 - 실행 횟수와 차수를 구해라 )
'''
total = 0
for i in range(1, x+1):         # 1 ~ x 까지의 합을 구하는 반복문
    total += i
'''
print(x)                        # 실행 횟수
print(1)                        # 빅 O 표현법으로 반복문 한번은 O(n)으로 표현 = 1차이므로 1을 출력
