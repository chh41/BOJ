x = int(input())                          # 시간 복잡도 문제 ( 실행 횟수와 차수 출력 )

'''
MenOfPassion(A[], n) {
    sum <- 0;
    for i <- 1 to n - 2
        for j <- i + 1 to n - 1
            for k <- j + 1 to n
                sum <- sum + A[i] × A[j] × A[k]; # 코드1
    return sum;
}
'''
a = (x-2) * (x-1) * x // 6                # 횟수 구하는 코드

print(a)                                  
print(3)                                  # 어쨋든 삼중 반복문이기에 3 출력
