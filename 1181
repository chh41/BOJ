import sys

input = sys.stdin.readline

x = int(input())

a = [''] * x
t = 0
for i in range(x):
    a[i] = str(input().strip())
a = list(set(a))
a.sort()
a.sort(key=len)                # key=len을 통해 len 순서로 정렬;;; 개사기네

for i in range(len(a)):
    print(a[i])
