x = int(input())                              # 시간 복잡도 문제 ( 실행 횟수와 차수 출력 ) 
a = 0
'''
MenOfPassion(A[], n) {
    sum <- 0;
    for i <- 1 to n - 1
        for j <- i + 1 to n
            sum <- sum + A[i] × A[j]; # 코드1
    return sum;
}
'''

for i in range(1,x):                          # 문제의 실행횟수가 1 ~ x -1 이기 때문에 반복문 사용
    a += i
    
print(a)
print(2)                                      # 결국 이중반복문이기때문에 차수는 2 O(n^2)
